def add(base: int, add: int) -> int:
    """
    Add two numbers

    Args:
        base: base number to add to
        add: number to add to base

    Returns:
        int
    """
    return base + int

